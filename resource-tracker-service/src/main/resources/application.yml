server:
  port: 8082

spring:
  application:
    name: resource-tracker-service
  datasource:
    url: jdbc:postgresql://localhost:5432/resource_tracker_db
    username: postgres
    password: 9834199283
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true